kind = "web"
previewPath = "/"
title = "Mister — Fantacalcio Manageriale"
version = "1.0.0"
id = "artifacts/mister-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19696

[services.development]
run = "pnpm --filter @workspace/mister-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mister-web", "run", "build" ]
publicDir = "artifacts/mister-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19696"
BASE_PATH = "/"
